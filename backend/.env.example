# Google Cloud Configuration
GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/compute-service-account-key.json
GOOGLE_CLOUD_PROJECT=gen-lang-client-0665888431
GCP_SERVICE_ACCOUNT_EMAIL=451921002283-compute@developer.gserviceaccount.com

# Gmail Configuration (using service account domain-wide delegation)
GMAIL_IMPERSONATE_USER=amit.gupta@coralbayadvisory.com
EMAIL_SENDER=noreply@coralbayadvisory.com
EMAIL_RECIPIENTS=amit.gupta@coralbayadvisory.com

# Database Configuration - Cloud SQL PostgreSQL (REQUIRED)
# Option 1: Using individual environment variables (recommended for Cloud Run)
CLOUD_SQL_HOST=35.200.192.16
CLOUD_SQL_PORT=5432
CLOUD_SQL_USER=postgres
CLOUD_SQL_PASSWORD=your-secure-password
CLOUD_SQL_DATABASE=inventory
CLOUD_SQL_CONNECTION_NAME=gen-lang-client-0665888431:asia-south1:crystal-inventory-dash
# For Cloud SQL Python Connector (local development and production)
CLOUD_SQL_REGION=asia-south1
CLOUD_SQL_INSTANCE=crystal-inventory-dash

# Option 2: Direct DATABASE_URL (if not using individual variables)
# DATABASE_URL=postgresql+asyncpg://postgres:password@host:port/database

# Google Cloud Storage
GCS_BUCKET_NAME=dashboard-inventory
GCS_PROJECT_ID=gen-lang-client-0665888431

# Generative AI (Gemini)
# Uses GOOGLE_API_KEY from environment if available, or set GOOGLE_GENAI_API_KEY
# GOOGLE_GENAI_API_KEY=your-genai-api-key

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
APP_BASE_URL=http://localhost:3000

# Application Environment
ENVIRONMENT=development
# ENVIRONMENT=production

# Logging
LOG_LEVEL=INFO

# CORS Configuration (update for production)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Email Configuration
EMAIL_RETRY_ATTEMPTS=3
EMAIL_RETRY_DELAY=300

# Scheduler Configuration
SCHEDULER_INTERVAL_MINUTES=1
REMINDER_THRESHOLD_MINUTES=120
CLOSURE_THRESHOLD_MINUTES=240

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
